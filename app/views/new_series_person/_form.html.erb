<%= form_for(@new_series_person) do |f| %>
  <% if @new_series_person.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@new_series_person.errors.count, "error") %> prohibited this blog from being saved:</h2>

      <ul>
      <% @blog.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :episode_id %><br>
    <%= f.text_field :episode_id %>
  </div>
  <div class="field">
    <%= f.label :opening_crawl %><br>
    <%= f.text_area :opening_crawl %>
  </div>
  <div class="field">
    <%= f.label :director %><br>
    <%= f.text_area :director %>
  </div>
  <div class="field">
    <%= f.label :producer %><br>
    <%= f.text_area :producer %>
  </div>
  <div class="field">
    <%= f.label :release_date %><br>
    <%= f.date_field :release_date %>
  </div>
  <div class="field">
    <%= f.label :new_series_person_id %><br>
    <%= f.text_field :new_series_person_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>


#    t.integer  "episode_id"
#    t.string   "opening_crawl"
#    t.string   "director"
#    t.string   "producer"
#    t.datetime "release_date"
#    t.integer  "new_series_person_id"
#    t.datetime "created_at",           null: false
#    t.datetime "updated_at",           null: false
