<%= form_for(@new_series_film) do |f| %>
  <% if @new_series_film.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@new_series_film.errors.count, "error") %> prohibited this new series film from being saved:</h2>

      <ul>
      <% @new_series_film.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :episode_id %><br>
    <%= f.text_field :episode_id %>
  </div>
  <div class="field">
    <%= f.label :opening_crawl %><br>
    <%= f.text_area :opening_crawl %>
  </div>
  <div class="field">
    <%= f.label :director %><br>
    <%= f.text_field :director %>
  </div>
  <div class="field">
    <%= f.label :producer %><br>
    <%= f.text_field :producer %>
  </div>
  <div class="field">
    <%= f.label :release_date %><br>
    <%= f.date_field :release_date %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
